{
  "version": 2,
  "functions": {
    "api/**/*.js": {
      "runtime": "nodejs18.x"
    }
  },
  "builds": [
    {
      "src": "podquote/client/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/static/(.*)",
      "dest": "/podquote/client/build/static/$1"
    },
    {
      "src": "/(.*\\.(js|css|map|json|ico|png|jpg|jpeg|gif|svg|woff|woff2|ttf|eot))",
      "dest": "/podquote/client/build/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/podquote/client/build/index.html"
    }
  ],
  "installCommand": "cd podquote/client && npm install && cd ../../api && npm install",
  "buildCommand": "cd podquote/client && npm run build"
} 