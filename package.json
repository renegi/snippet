{
  "name": "snippet",
  "version": "1.0.0",
  "description": "Snippet - AI-powered podcast quote extraction from screenshots",
  "scripts": {
    "build": "cd snippet/client && npm install && npm run build",
    "start": "cd snippet/server && npm install && npm start",
    "install-all": "cd snippet/client && npm install && cd ../server && npm install",
    "dev": "cd snippet/server && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "sharp": "^0.34.3"
  }
}
