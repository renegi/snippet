{
  "version": 2,
  "buildCommand": "cd podquote/client && npm install && npm run build",
  "outputDirectory": "podquote/client/build",
  "installCommand": "npm install --prefix podquote/client && npm install --prefix podquote/server",
  "functions": {
    "podquote/server/server.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/podquote/server/server.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
} 